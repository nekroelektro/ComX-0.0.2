@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    string currentUrl = Request.Url?.AbsoluteUri;
    bool isDiaryDocument = currentUrl.Contains("#");
}
<div class="body-content">
    @if (currentUrl.Contains("+") || currentUrl.Contains("#")) {
        @Html.Action("_TopDetailPanel", new {isDiary = isDiaryDocument})
    }
    <div class="mainBodyContainer">
        @RenderBody()
    </div>
    @if (!currentUrl.Contains("Create") && !currentUrl.Contains("Edit")) {
        <div class="mainSideBar">
            @Html.Partial("_SideBar")
        </div>
    }
    <div class="push"></div>
</div>
<script>
    //$(document).ready(function () {
    //    var eventFired = 0;

    //    if ($(window).width() < 1070) {
    //        eventFired = 1;
    //    }
    //    if ($(window).width() < 1070) {
    //    }

    //    $(window).on('resize', function () {
    //        if (!eventFired) {
    //            if ($(window).width() < 1120) {
    //                $(".mainSideBar").css('display', 'none');
    //                $(".mainBodyContainer").css('width', '100%');
    //            } else {
    //                $(".mainSideBar").css('display', 'block');
    //                $(".mainBodyContainer").css('width', '69%');
    //            }
    //        }
    //    });
    //});

</script>

@*SCRIPTS FOR VIEW*@
<script type="text/javascript" src="~/Views/Articles/Scripts/Articles.js"></script>
<link href="~/Views/Articles/Styles/_Articles.css" rel="stylesheet" type="text/css" />