@using ComX_0._0._2.Helpers
@using React.AspNet
@model ComX_0._0._2.Views.Articles.Models.DtoModels.CommentDetailsDto
@{
    var userHelper = new UserHelper();

    bool isCurrentUserLoggedIn = !string.IsNullOrEmpty(User.Identity.Name);
    bool isCurrentUserBlocked = userHelper.IsCurrentUserBlocked();
}
@Html.React("Comments", new {model=Model, logged=isCurrentUserLoggedIn, blocked=isCurrentUserBlocked})
