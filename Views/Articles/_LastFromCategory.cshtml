@using ComX_0._0._2.Helpers
@model IEnumerable<ComX_0._0._2.Views.Articles.Models.Articles>
@{
    ArticleHelper articleHelper = new ArticleHelper();
    GeneralHelper generalHelper = new GeneralHelper();
}

@if (Model.Any()){
    var categoryName = articleHelper.GetCategoryById(Model.First().CategoryId).Name;
    <div class="lastFromCategoryPanel">
        <div class="lastFromCategoryLabel">
            Zobacz ostatnie z tej kategorii:
        </div>
        <div class="lastFromCategoryContainer">
            @foreach (var item in Model)
            {
                <a href="@HttpUtility.UrlDecode(Url.Action("Details", "Articles", new {@id = generalHelper.RemoveSpecialCharsFromString(item.Name)}))">
                    <div class="singleLastArticleTopNavigation">
                        <div class="imageOverlayColorLast"></div>
                        <div class="bannerPanelImageContainerLast">
                            <img src='@Url.Content(articleHelper.GetImageRelativePathByArticleId(item.Id))'/>
                        </div>
                        <div class="bannerPanelImageMainLast">
                            <img src='@Url.Content(articleHelper.GetImageRelativePathByArticleId(item.Id))'/>
                        </div>
                        <div class="titleTopNavigation">
                            <p>@item.Name</p>
                        </div>
                    </div>
                </a>
            }
        </div>
        <div class="lastFromCategoryLabelLinkContainer">
            <a class="seeMoreFromCategory" href="@Url.Action("Categories", "Articles", new {@id = categoryName})">
                <p> Zobacz wszystkie z kategorii @articleHelper.GetCategoryById(Model.First().CategoryId).Name <span class="glyphicon glyphicon-circle-arrow-right" aria-hidden="true"></span></p>
            </a>
        </div>
    </div>
}

@*SCRIPTS FOR VIEW*@
<link href="~/Views/Articles/Styles/_LastFromCategory.css" rel="stylesheet" type="text/css" />